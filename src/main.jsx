import React, { useCallback, useEffect, useRef, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { Camera, Circle, Hand, MousePointer2, PenLine, Play, Sparkles, Square, Zap } from 'lucide-react';
import { HandLandmarker, FilesetResolver } from '@mediapipe/tasks-vision';
import './styles.css';

const MP_VERSION = '0.10.35';
const WASM_URL = `https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@${MP_VERSION}/wasm`;
const MODEL_URL = 'https://storage.googleapis.com/mediapipe-models/hand_landmarker/hand_landmarker/float16/1/hand_landmarker.task';
const BACKEND_WS = import.meta.env.VITE_VISION_WS_URL || '';
const MODES = { draw:{label:'Draw',icon:PenLine,hint:'Freehand ink'}, shape:{label:'Shape',icon:Sparkles,hint:'Align rough shapes'}, pointer:{label:'Pointer',icon:MousePointer2,hint:'Point anywhere'}, laser:{label:'Laser',icon:Zap,hint:'Highlight briefly'} };
const dist=(a,b)=>Math.hypot(a.x-b.x,a.y-b.y);
function angle(a,b,c){const ab={x:a.x-b.x,y:a.y-b.y},cb={x:c.x-b.x,y:c.y-b.y},d=(Math.hypot(ab.x,ab.y)*Math.hypot(cb.x,cb.y))||1;return Math.acos(Math.max(-1,Math.min(1,(ab.x*cb.x+ab.y*cb.y)/d)))*180/Math.PI}
function fingerExtended(h,m,p,t){const pip=angle(h[m],h[p],h[t]);const reach=dist(h[m],h[t]);const segment=dist(h[m],h[p]);return pip>138&&reach>segment*1.03}
function classifyGesture(h){if(!h||h.length!==21)return'none';const i=fingerExtended(h,5,6,8),m=fingerExtended(h,9,10,12),r=fingerExtended(h,13,14,16),p=fingerExtended(h,17,18,20);if(i&&!m&&!r&&!p)return'draw';if(i&&m&&r&&p)return'clear';if(!i&&!m&&!r&&!p)return'pause';return'none'}
function perpendicular(p,a,b){const dx=b.x-a.x,dy=b.y-a.y;if(!dx&&!dy)return dist(p,a);return Math.abs(dy*p.x-dx*p.y+b.x*a.y-b.y*a.x)/Math.hypot(dx,dy)}
function simplify(points,epsilon){if(points.length<=2)return points.slice();let best=1,max=perpendicular(points[1],points[0],points.at(-1));for(let i=2;i<points.length-1;i++){const d=perpendicular(points[i],points[0],points.at(-1));if(d>max){max=d;best=i}}if(max>epsilon)return simplify(points.slice(0,best+1),epsilon).slice(0,-1).concat(simplify(points.slice(best),epsilon));return[points[0],points.at(-1)]}
function detectShape(points){if(points.length<12)return null;const start=points[0],end=points.at(-1),xs=points.map(p=>p.x),ys=points.map(p=>p.y),minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys),scale=Math.max(maxX-minX,maxY-minY,1);if(dist(start,end)>scale*.28)return{type:'line',from:start,to:end,sides:2};const ring=points.slice(0,-1),cx=ring.reduce((s,p)=>s+p.x,0)/ring.length,cy=ring.reduce((s,p)=>s+p.y,0)/ring.length,radius=ring.reduce((s,p)=>s+Math.hypot(p.x-cx,p.y-cy),0)/ring.length;if(radius<4)return null;const radial=ring.map(p=>Math.hypot(p.x-cx,p.y-cy)),mean=radial.reduce((s,v)=>s+v,0)/radial.length,radialError=Math.sqrt(radial.reduce((s,v)=>s+(v-mean)**2,0)/radial.length)/Math.max(mean,1);let corners=simplify(ring.concat([ring[0]]),Math.max(4,scale*.075));if(corners.length>1&&dist(corners[0],corners.at(-1))<scale*.2)corners=corners.slice(0,-1);let n=Math.max(3,Math.min(12,corners.length));if(radialError<.10||n>=9)n=64;if(n===64)return{type:'circle',cx,cy,r:radius,sides:0};const startAngle=Math.atan2(corners[0].y-cy,corners[0].x-cx),vertices=Array.from({length:n},(_,i)=>({x:cx+radius*Math.cos(startAngle+2*Math.PI*i/n),y:cy+radius*Math.sin(startAngle+2*Math.PI*i/n)}));return{type:n===3?'triangle':n===4?'square':n===5?'pentagon':n===6?'hexagon':n===7?'heptagon':n===8?'octagon':`${n}-gon`,sides:n,vertices}}

function App(){
 const video=useRef(null),canvas=useRef(null),tracker=useRef(null),stream=useRef(null),raf=useRef(null),previous=useRef(null),smooth=useRef(null),points=useRef([]),lastFrame=useRef(0),lastSeen=useRef(0),clearAt=useRef(0),trail=useRef([]),gestureHistory=useRef([]),stableGesture=useRef('none'),ws=useRef(null),backendGesture=useRef('');
 const[camera,setCamera]=useState(false),[ready,setReady]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState(''),[mode,setMode]=useState('draw'),[g,setG]=useState('none'),[tip,setTip]=useState(null),[aligned,setAligned]=useState('');
 const resize=useCallback(()=>{const c=canvas.current,v=video.current;if(!c||!v)return;const r=v.getBoundingClientRect(),d=Math.min(window.devicePixelRatio||1,2);c.width=Math.max(1,Math.round(r.width*d));c.height=Math.max(1,Math.round(r.height*d));c.style.width=`${r.width}px`;c.style.height=`${r.height}px`;c.getContext('2d').setTransform(d,0,0,d,0,0)},[]);
 const wipe=useCallback(()=>{const c=canvas.current;if(c)c.getContext('2d').clearRect(0,0,c.clientWidth,c.clientHeight);previous.current=null;smooth.current=null;points.current=[];trail.current=[];gestureHistory.current=[];stableGesture.current='none';setAligned('')},[]);
 const segment=useCallback((a,b)=>{const x=canvas.current?.getContext('2d');if(!x||mode==='pointer')return;const n=Math.max(1,Math.ceil(dist(a,b)/2.5));x.save();x.strokeStyle='#f4f1e8';x.lineWidth=mode==='shape'?5:6;x.lineCap='round';x.lineJoin='round';x.shadowColor='#f4f1e8';x.shadowBlur=mode==='draw'?4:7;x.beginPath();x.moveTo(a.x,a.y);for(let i=1;i<=n;i++){const t=i/n;x.lineTo(a.x+(b.x-a.x)*t,a.y+(b.y-a.y)*t)}x.stroke();x.restore()},[mode]);
 const laser=useCallback(p=>{const c=canvas.current,x=c?.getContext('2d');if(!x)return;trail.current.push({...p,t:performance.now()});trail.current=trail.current.slice(-30);x.clearRect(0,0,c.clientWidth,c.clientHeight);for(let i=1;i<trail.current.length;i++){const a=trail.current[i-1],b=trail.current[i],alpha=Math.max(0,1-(performance.now()-b.t)/600);x.save();x.globalAlpha=alpha;x.strokeStyle='#e97878';x.shadowColor='#e97878';x.shadowBlur=14;x.lineWidth=3+alpha*4;x.lineCap='round';x.beginPath();x.moveTo(a.x,a.y);x.lineTo(b.x,b.y);x.stroke();x.restore()}},[]);
 const align=useCallback(()=>{if(mode!=='shape'||points.current.length<12)return;const s=detectShape(points.current),x=canvas.current?.getContext('2d');if(!s||!x)return;x.save();x.strokeStyle='#d8e6c9';x.lineWidth=5;x.lineCap='round';x.lineJoin='round';x.shadowColor='#d8e6c9';x.shadowBlur=8;x.beginPath();if(s.type==='circle')x.arc(s.cx,s.cy,s.r,0,Math.PI*2);else if(s.type==='line'){x.moveTo(s.from.x,s.from.y);x.lineTo(s.to.x,s.to.y)}else{s.vertices.forEach((p,i)=>i?x.lineTo(p.x,p.y):x.moveTo(p.x,p.y));x.closePath()}x.stroke();x.restore();setAligned(`${s.type} aligned${s.sides?` · ${s.sides} sides`:''}`);points.current=[]},[mode]);
 const init=async delegate=>{const vision=await FilesetResolver.forVisionTasks(WASM_URL);return HandLandmarker.createFromOptions(vision,{baseOptions:{modelAssetPath:MODEL_URL,delegate},runningMode:'VIDEO',numHands:1,minHandDetectionConfidence:.22,minHandPresenceConfidence:.22,minTrackingConfidence:.22})};
 const openBackend=useCallback(()=>{if(!BACKEND_WS||typeof WebSocket==='undefined')return;try{const socket=new WebSocket(BACKEND_WS);socket.onmessage=e=>{try{const data=JSON.parse(e.data);backendGesture.current=String(data.gesture||'').toLowerCase()}catch{}};socket.onerror=()=>socket.close();ws.current=socket}catch{ws.current=null}},[]);
 const closeBackend=useCallback(()=>{if(ws.current){try{ws.current.close()}catch{}ws.current=null}backendGesture.current=''},[]);
 const stop=useCallback(()=>{if(raf.current)cancelAnimationFrame(raf.current);raf.current=null;closeBackend();stream.current?.getTracks().forEach(t=>t.stop());stream.current=null;if(video.current)video.current.srcObject=null;tracker.current?.close();tracker.current=null;setCamera(false);setReady(false);setTip(null);setG('none');previous.current=null;smooth.current=null;gestureHistory.current=[];stableGesture.current='none'},[closeBackend]);
 const start=async()=>{if(!navigator.mediaDevices?.getUserMedia)return setError('Camera access is not supported by this browser.');if(!window.isSecureContext)return setError('Camera access requires HTTPS.');try{setError('');setLoading(true);const s=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'user'},width:{ideal:1280},height:{ideal:720},frameRate:{ideal:30,max:30}},audio:false});stream.current=s;video.current.srcObject=s;await video.current.play();setCamera(true);setLoading(false);requestAnimationFrame(resize);try{tracker.current=await init('GPU')}catch(e){console.warn('GPU vision unavailable, using CPU',e);tracker.current=await init('CPU')}openBackend();setReady(true)}catch(e){setLoading(false);setError(e?.name==='NotAllowedError'?'Camera permission was denied. Allow camera access and try again.':e?.message||'Camera could not start.');stream.current?.getTracks().forEach(t=>t.stop());stream.current=null}};
 const mapTipToSurface=useCallback(landmark=>{const c=canvas.current,v=video.current;if(!c||!v)return null;const cw=c.clientWidth,ch=c.clientHeight,vw=v.videoWidth||1280,vh=v.videoHeight||720,scale=Math.max(cw/vw,ch/vh),rw=vw*scale,rh=vh*scale,ox=(cw-rw)/2,oy=(ch-rh)/2;return{x:Math.max(0,Math.min(cw,(1-landmark.x)*rw+ox)),y:Math.max(0,Math.min(ch,landmark.y*rh+oy))}},[]);
 const stableFrom=useCallback(candidate=>{const history=gestureHistory.current;history.push(candidate);if(history.length>5)history.shift();const counts=history.reduce((a,k)=>(a[k]=(a[k]||0)+1,a),{}),best=Object.entries(counts).sort((a,b)=>b[1]-a[1])[0]?.[0]||'none';if((counts[best]||0)>=3)stableGesture.current=best;return stableGesture.current},[]);
 const frame=useCallback(()=>{if(!camera){raf.current=null;return}const v=video.current,l=tracker.current,now=performance.now();try{if(v&&l&&v.readyState>=2&&now-lastFrame.current>=30){lastFrame.current=now;const result=l.detectForVideo(v,now),h=result?.landmarks?.[0];if(h){lastSeen.current=now;const raw=mapTipToSurface(h[8]);if(raw){const old=smooth.current||raw,delta=dist(raw,old),alpha=delta>80?.9:delta>35?.76:.58,p={x:old.x+(raw.x-old.x)*alpha,y:old.y+(raw.y-old.y)*alpha};smooth.current=p;setTip(p);const localState=stableFrom(classifyGesture(h));const state=backendGesture.current||localState;setG(state);if(ws.current?.readyState===WebSocket.OPEN){try{ws.current.send(JSON.stringify({landmarks:h.map(q=>({x:q.x,y:q.y,z:q.z||0})),timestamp:now/1000,confidence:1}))}catch{}}if(state==='clear'){if(!clearAt.current)clearAt.current=now;if(now-clearAt.current>700){clearAt.current=0;wipe()}previous.current=null}else if(state==='pause'){if(mode==='shape')align();previous.current=null}else if(state==='draw'){clearAt.current=0;if(mode==='laser'){laser(p);previous.current=p}else{if(previous.current)segment(previous.current,p);previous.current=p;if(mode==='shape')points.current.push(p)}}else if(mode==='pointer'){previous.current=p}else{previous.current=null}}}else if(now-lastSeen.current>350){setTip(null);setG('none');previous.current=null;smooth.current=null;gestureHistory.current=[];stableGesture.current='none';backendGesture.current=''}}}catch(e){console.warn('AirCanvas vision frame',e)}finally{raf.current=requestAnimationFrame(frame)}},[camera,mode,segment,laser,align,wipe,stableFrom,mapTipToSurface]);
 useEffect(()=>{if(camera)raf.current=requestAnimationFrame(frame);return()=>{if(raf.current)cancelAnimationFrame(raf.current)}},[camera,frame]);
 useEffect(()=>{window.addEventListener('resize',resize);return()=>window.removeEventListener('resize',resize)},[resize]);
 useEffect(()=>()=>stop(),[stop]);
 const M=MODES[mode];
 return <main className="site"><header className="nav"><div className="wordmark"><span className="mark">A</span><div><strong>AirCanvas</strong><small>gesture studio / 001</small></div></div><div className="nav-meta"><span className={ready?'engine live':'engine'}><i/>{ready?'VISION LIVE':'VISION OFF'}</span><span>PASSION PROJECT</span></div></header><section className="hero"><div className="hero-copy"><p className="kicker">HUMAN · COMPUTER · INTERACTION</p><h1>Put the idea<br/><em>in the air.</em></h1><p className="lead">Your hand becomes the interface. Draw, point, align shapes or highlight ideas without touching the screen.</p><div className="actions">{!camera?<button className="start" onClick={start} disabled={loading}>{loading?<Circle className="spin" size={16}/>:<Play size={16} fill="currentColor"/>}{loading?'Opening camera':'Open camera'}</button>:<button className="stop" onClick={stop}><Square size={14} fill="currentColor"/> Stop</button>}<span><Hand size={15}/> one hand · no mouse</span></div>{error&&<p className="error">{error}</p>}<div className="hero-promise"><span>☝ DRAW</span><span>◇ ALIGN</span><span>⌖ POINT</span><span>⚡ LASER</span></div></div><div className="hero-camera"><div className="camera-top"><span><i className={camera?'on':''}/>{camera?(ready?'VISION TRACKING LIVE':'STARTING VISION'):'LIVE CAMERA'}</span><span>01 / USP</span></div><div className="surface"><video ref={video} className="video" playsInline muted autoPlay/><canvas ref={canvas} className="canvas"/>{tip&&camera&&<div className={`reticle ${mode==='pointer'||g==='draw'?'active':''}`} style={{left:tip.x,top:tip.y}}><span/></div>}{!camera&&<div className="empty"><div className="empty-icon"><Camera size={23}/></div><strong>See AirCanvas in action.</strong><span>Open camera · raise one finger · move.</span></div>}<div className="surface-label">{camera?`${M.label.toUpperCase()} · ${mode==='pointer'?(tip?'TRACKING':'SEARCHING'):(g==='draw'?'LIVE':g==='pause'?'PAUSED':'READY')}`:'YOUR HAND IS THE CONTROLLER'}</div>{aligned&&<div className="shape-toast">✓ {aligned}</div>}</div><div className="camera-bottom"><span>☝ draw</span><span>✊ pause / align</span><span>🖐 clear</span></div></div></section><section className="instruments"><div><small>02 / CHOOSE YOUR INSTRUMENT</small><h2>One surface.<br/>Four ways to explain.</h2></div><div className="mode-grid">{Object.entries(MODES).map(([key,item])=>{const I=item.icon;return <button key={key} className={mode===key?'active':''} onClick={()=>{setMode(key);previous.current=null;points.current=[];trail.current=[]}}><I size={17}/><b>{item.label}</b><small>{item.hint}</small></button>})}</div></section><section className="build"><div><small>03 / VISION FIRST</small><h2>Camera → landmarks →<br/>finger point.</h2></div><p>Shape mode turns rough closed strokes into regular geometry: three detected corners become a clean triangle, four become a square, five a pentagon, and so on. Round closed strokes become circles. Hold the pause gesture to commit the correction.</p></section><footer>Made by - Amar <span>·</span> AirCanvas / passion project 001</footer></main>;
}

createRoot(document.getElementById('root')).render(<App/>);
